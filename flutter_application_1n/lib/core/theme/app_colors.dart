import 'package:flutter/material.dart';

class AppColors {
  // الألوان الأساسية (بناءً على تصميمك البنفسجي)
  static const Color primary = Color(0xFF6C63FF);
  static const Color primaryLight = Color(0xFF8E87FF);
  static const Color accent = Color(0xFFFF5252); 
  static const Color secondary = Color(0xFFFFA000);
  // للتحذيرات والحالات الخطرة

  // ألوان الخلفيات والنصوص
  static const Color background = Color(0xFFF8F9FE);
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF2D2D2D);
  static const Color textSecondary = Color(0xFF757575);
  
  // ألوان الحالات
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFD50000);
}